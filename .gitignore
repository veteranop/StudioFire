# Python
__pycache__/
*.pyc
.venv/
venv/

# Runtime state — never commit
logs/
data/
*.db
*.db-wal
*.db-shm
precache/
config/config.json

# Engine runtime state
queue_state.json
heartbeat.txt

# Binaries (fetched by installer, not vendored)
bin/

# Local media assets (user-managed)
media/

# OS
Thumbs.db
desktop.ini
